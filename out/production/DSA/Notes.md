* int k = String.valueOf(num).length(); // Get number of digits

    String.valueOf(num)
     - This converts the integer number into a String.
    
    .length()
     - length() gives the number of characters in the string.


* Math.pow(ld, k) - Math.pow(base, exponent)
 - ld raised to the power k
 - ld^k





